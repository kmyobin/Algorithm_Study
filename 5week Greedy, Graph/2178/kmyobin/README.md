## Info
<a href="https://www.acmicpc.net/problem/2178" rel="nofollow">2178 미로 탐색</a>


## 풀이
`BFS`로 풀었다.

최단 경로를 어떻게 표시할까 고민이 많았는데, 전 탐색값에서 `+1`을 해주며 탐색을 하니 `A[N][M]`에서 정답이 나왔다.

나머지는 일반 `BFS` 코드와 동일하다.



## 마무리
`BFS`와 `DFS`는 대부분의 코딩 테스트에 나오는 것 같다. 공부 열심히 해야겠다.