## Info
<a href="https://www.acmicpc.net/problem/1339" rel="nofollow">1339 단어 수학</a>


## 풀이
처음에는 단순히 자릿수가 큰 순서대로 `9~0`을 부여했으나 오답이 떴다.

알고보니 각 자릿수마다 `10^n`씩 가중치를 부여한 다음, 내림차순 정렬하여 `9~0`을 부여해야했다.

`구조체`를 만들어서 `알파벳순 정렬`, `가중치순 정렬` 함수를 따로 만들었다.



## 마무리
가중치를 부여하는 과정에서 잘못 생각했었다.